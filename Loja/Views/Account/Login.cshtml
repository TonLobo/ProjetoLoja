@{
    ViewBag.Title = "Login";
}

<h2>Acesso</h2>

@using (Html.BeginForm())
{
    <div>
        @Html.Label("Usuário")
        @Html.TextBox("username")
    </div>
    <div>
        @Html.Label("Senha")
        @Html.Password("password")
    </div>
    <button type="submit">Entrar</button>
}

<p>@ViewBag.Message</p>